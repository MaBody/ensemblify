from setuptools import setup

setup(
    name="ensemblify",
    version="1.0",
    description="MSA ensembles but make it easy.",
    author="Mattis Bodynek",
    author_email="mattisbodynek@gmail.com",
    packages=["ensemblify"],  
    install_requires=[
        # add
    ],
)
